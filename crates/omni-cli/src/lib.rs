//! Command-line host: the first deliverable. Execution before UI.
//!
//! Empty by design: created by Task 1 so later tasks have somewhere to land. A `[[bin]]` target
//! is deliberately not added yet, because an entry point that does nothing would be placeholder
//! success (Global Constraint 1).
#![forbid(unsafe_code)]
