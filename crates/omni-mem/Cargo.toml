[package]
name = "omni-mem"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[dependencies]
omni-platform = { workspace = true }
thiserror = { workspace = true }
parking_lot = { workspace = true }
tracing = { workspace = true }

# Linux tests only: `sigaltstack(2)`, to measure how much alternate stack the demand pager's
# SIGSEGV path really uses (`tests/pager_linux`). Nothing in this crate's own code calls the OS.
[target.'cfg(target_os = "linux")'.dev-dependencies]
libc = "0.2"
